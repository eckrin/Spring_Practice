#??? ????

server:
    port: 8080
    servlet:
        context-path: /
        encoding:
            charset: UTF-8
            enabled: true
            force: true

spring:
    mvc:
        view:
            prefix: /WEB-INF/views/
            suffix: .jsp
        #throw-exception-if-no-handler-found: true #404에러 캐치
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/yourssu_blog?serverTimezone=Asia/Seoul
        username: root
        password: dudqls11@

    jpa:
        open-in-view: true
        hibernate:
            ddl-auto: update
            naming:
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # ??? ??? ??? ??? db? ???
            use-new-id-generator-mappings: false
#    show-sql: true
        properties:
            hibernate.format_sql: true

    jackson:
        serialization:
            fail-on-empty-beans: false